# ========================================
# TRAVEL SCOPE PROBE - JE BUTTON (DEBUG / DISPOSABLE)
# Runs in JE root scope (owning country) - a BASELINE.
# Uses error_log so output always lands in error.log.
# ========================================

stl_travel_scope_test_button = {
	visible = { always = yes }

	effect = {
		error_log = "STL_TRAVEL_PROBE je_button_country_baseline"
		stl_travel_scope_probe = yes

		every_military_formation = {
			error_log = "STL_TRAVEL_PROBE formation_baseline"
			stl_travel_scope_probe = yes
		}
	}
}
