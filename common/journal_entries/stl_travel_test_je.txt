# ========================================
# TRAVEL SCOPE PROBE - JOURNAL ENTRY (DEBUG / DISPOSABLE)
# Always-active per-country JE so the player gets a clickable button.
# Never completes - it's just a debug panel.
# ========================================

je_stl_travel_scope_test = {
	group = journalentrygroup_stl_travel_test
	icon = "gfx/interface/icons/event_icons/event_industry.dds"

	# Renders the probe button in the JE panel.
	scripted_button = stl_travel_scope_test_button

	# Activate for every country (player sees their own).
	is_shown_when_inactive = { always = yes }
	possible = { always = yes }

	immediate = {
		debug_log = "STL_TRAVEL_PROBE je_activated"
	}

	# Never auto-completes: stays available as a debug panel.
	complete = { always = no }
}
