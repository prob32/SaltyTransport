# AUTO-GENERATED by tools/generate_goods.py -- DO NOT EDIT
# Regenerate: python3 tools/generate_goods.py

# Thin per-good readers for GUI data binding.
# Scope: state (via GuiScope.SetRoot(...State.MakeScope))
# Bound from gui as: ScriptValue(Concatenate('stl_gui_<metric>_', Goods.GetKey))

# === surplus (stl_eff_export) ===
stl_gui_surplus_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:grain } } add = variable_map(stl_eff_export|flag:grain) } }
stl_gui_surplus_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fish } } add = variable_map(stl_eff_export|flag:fish) } }
stl_gui_surplus_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fabric } } add = variable_map(stl_eff_export|flag:fabric) } }
stl_gui_surplus_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:wood } } add = variable_map(stl_eff_export|flag:wood) } }
stl_gui_surplus_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:groceries } } add = variable_map(stl_eff_export|flag:groceries) } }
stl_gui_surplus_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:clothes } } add = variable_map(stl_eff_export|flag:clothes) } }
stl_gui_surplus_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:furniture } } add = variable_map(stl_eff_export|flag:furniture) } }
stl_gui_surplus_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:paper } } add = variable_map(stl_eff_export|flag:paper) } }
stl_gui_surplus_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:merchant_marine } } add = variable_map(stl_eff_export|flag:merchant_marine) } }
stl_gui_surplus_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:coal } } add = variable_map(stl_eff_export|flag:coal) } }
stl_gui_surplus_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:iron } } add = variable_map(stl_eff_export|flag:iron) } }
stl_gui_surplus_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:lead } } add = variable_map(stl_eff_export|flag:lead) } }
stl_gui_surplus_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:sulfur } } add = variable_map(stl_eff_export|flag:sulfur) } }
stl_gui_surplus_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:hardwood } } add = variable_map(stl_eff_export|flag:hardwood) } }
stl_gui_surplus_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:rubber } } add = variable_map(stl_eff_export|flag:rubber) } }
stl_gui_surplus_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:oil } } add = variable_map(stl_eff_export|flag:oil) } }
stl_gui_surplus_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:silk } } add = variable_map(stl_eff_export|flag:silk) } }
stl_gui_surplus_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:dye } } add = variable_map(stl_eff_export|flag:dye) } }
stl_gui_surplus_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:clippers } } add = variable_map(stl_eff_export|flag:clippers) } }
stl_gui_surplus_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:steamers } } add = variable_map(stl_eff_export|flag:steamers) } }
stl_gui_surplus_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:glass } } add = variable_map(stl_eff_export|flag:glass) } }
stl_gui_surplus_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fertilizer } } add = variable_map(stl_eff_export|flag:fertilizer) } }
stl_gui_surplus_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tools } } add = variable_map(stl_eff_export|flag:tools) } }
stl_gui_surplus_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:steel } } add = variable_map(stl_eff_export|flag:steel) } }
stl_gui_surplus_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:engines } } add = variable_map(stl_eff_export|flag:engines) } }
stl_gui_surplus_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:explosives } } add = variable_map(stl_eff_export|flag:explosives) } }
stl_gui_surplus_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:ammunition } } add = variable_map(stl_eff_export|flag:ammunition) } }
stl_gui_surplus_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:small_arms } } add = variable_map(stl_eff_export|flag:small_arms) } }
stl_gui_surplus_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:artillery } } add = variable_map(stl_eff_export|flag:artillery) } }
stl_gui_surplus_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tanks } } add = variable_map(stl_eff_export|flag:tanks) } }
stl_gui_surplus_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:aeroplanes } } add = variable_map(stl_eff_export|flag:aeroplanes) } }
stl_gui_surplus_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:meat } } add = variable_map(stl_eff_export|flag:meat) } }
stl_gui_surplus_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fruit } } add = variable_map(stl_eff_export|flag:fruit) } }
stl_gui_surplus_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:sugar } } add = variable_map(stl_eff_export|flag:sugar) } }
stl_gui_surplus_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tobacco } } add = variable_map(stl_eff_export|flag:tobacco) } }
stl_gui_surplus_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:liquor } } add = variable_map(stl_eff_export|flag:liquor) } }
stl_gui_surplus_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:wine } } add = variable_map(stl_eff_export|flag:wine) } }
stl_gui_surplus_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tea } } add = variable_map(stl_eff_export|flag:tea) } }
stl_gui_surplus_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:coffee } } add = variable_map(stl_eff_export|flag:coffee) } }
stl_gui_surplus_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:opium } } add = variable_map(stl_eff_export|flag:opium) } }
stl_gui_surplus_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:porcelain } } add = variable_map(stl_eff_export|flag:porcelain) } }
stl_gui_surplus_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:luxury_clothes } } add = variable_map(stl_eff_export|flag:luxury_clothes) } }
stl_gui_surplus_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:luxury_furniture } } add = variable_map(stl_eff_export|flag:luxury_furniture) } }
stl_gui_surplus_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:automobiles } } add = variable_map(stl_eff_export|flag:automobiles) } }
stl_gui_surplus_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:telephones } } add = variable_map(stl_eff_export|flag:telephones) } }
stl_gui_surplus_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:radios } } add = variable_map(stl_eff_export|flag:radios) } }
stl_gui_surplus_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fine_art } } add = variable_map(stl_eff_export|flag:fine_art) } }

# === deficit (stl_eff_import) ===
stl_gui_deficit_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:grain } } add = variable_map(stl_eff_import|flag:grain) } }
stl_gui_deficit_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fish } } add = variable_map(stl_eff_import|flag:fish) } }
stl_gui_deficit_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fabric } } add = variable_map(stl_eff_import|flag:fabric) } }
stl_gui_deficit_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:wood } } add = variable_map(stl_eff_import|flag:wood) } }
stl_gui_deficit_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:groceries } } add = variable_map(stl_eff_import|flag:groceries) } }
stl_gui_deficit_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:clothes } } add = variable_map(stl_eff_import|flag:clothes) } }
stl_gui_deficit_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:furniture } } add = variable_map(stl_eff_import|flag:furniture) } }
stl_gui_deficit_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:paper } } add = variable_map(stl_eff_import|flag:paper) } }
stl_gui_deficit_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:merchant_marine } } add = variable_map(stl_eff_import|flag:merchant_marine) } }
stl_gui_deficit_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:coal } } add = variable_map(stl_eff_import|flag:coal) } }
stl_gui_deficit_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:iron } } add = variable_map(stl_eff_import|flag:iron) } }
stl_gui_deficit_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:lead } } add = variable_map(stl_eff_import|flag:lead) } }
stl_gui_deficit_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:sulfur } } add = variable_map(stl_eff_import|flag:sulfur) } }
stl_gui_deficit_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:hardwood } } add = variable_map(stl_eff_import|flag:hardwood) } }
stl_gui_deficit_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:rubber } } add = variable_map(stl_eff_import|flag:rubber) } }
stl_gui_deficit_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:oil } } add = variable_map(stl_eff_import|flag:oil) } }
stl_gui_deficit_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:silk } } add = variable_map(stl_eff_import|flag:silk) } }
stl_gui_deficit_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:dye } } add = variable_map(stl_eff_import|flag:dye) } }
stl_gui_deficit_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:clippers } } add = variable_map(stl_eff_import|flag:clippers) } }
stl_gui_deficit_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:steamers } } add = variable_map(stl_eff_import|flag:steamers) } }
stl_gui_deficit_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:glass } } add = variable_map(stl_eff_import|flag:glass) } }
stl_gui_deficit_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fertilizer } } add = variable_map(stl_eff_import|flag:fertilizer) } }
stl_gui_deficit_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tools } } add = variable_map(stl_eff_import|flag:tools) } }
stl_gui_deficit_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:steel } } add = variable_map(stl_eff_import|flag:steel) } }
stl_gui_deficit_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:engines } } add = variable_map(stl_eff_import|flag:engines) } }
stl_gui_deficit_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:explosives } } add = variable_map(stl_eff_import|flag:explosives) } }
stl_gui_deficit_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:ammunition } } add = variable_map(stl_eff_import|flag:ammunition) } }
stl_gui_deficit_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:small_arms } } add = variable_map(stl_eff_import|flag:small_arms) } }
stl_gui_deficit_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:artillery } } add = variable_map(stl_eff_import|flag:artillery) } }
stl_gui_deficit_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tanks } } add = variable_map(stl_eff_import|flag:tanks) } }
stl_gui_deficit_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:aeroplanes } } add = variable_map(stl_eff_import|flag:aeroplanes) } }
stl_gui_deficit_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:meat } } add = variable_map(stl_eff_import|flag:meat) } }
stl_gui_deficit_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fruit } } add = variable_map(stl_eff_import|flag:fruit) } }
stl_gui_deficit_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:sugar } } add = variable_map(stl_eff_import|flag:sugar) } }
stl_gui_deficit_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tobacco } } add = variable_map(stl_eff_import|flag:tobacco) } }
stl_gui_deficit_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:liquor } } add = variable_map(stl_eff_import|flag:liquor) } }
stl_gui_deficit_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:wine } } add = variable_map(stl_eff_import|flag:wine) } }
stl_gui_deficit_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tea } } add = variable_map(stl_eff_import|flag:tea) } }
stl_gui_deficit_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:coffee } } add = variable_map(stl_eff_import|flag:coffee) } }
stl_gui_deficit_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:opium } } add = variable_map(stl_eff_import|flag:opium) } }
stl_gui_deficit_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:porcelain } } add = variable_map(stl_eff_import|flag:porcelain) } }
stl_gui_deficit_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:luxury_clothes } } add = variable_map(stl_eff_import|flag:luxury_clothes) } }
stl_gui_deficit_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:luxury_furniture } } add = variable_map(stl_eff_import|flag:luxury_furniture) } }
stl_gui_deficit_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:automobiles } } add = variable_map(stl_eff_import|flag:automobiles) } }
stl_gui_deficit_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:telephones } } add = variable_map(stl_eff_import|flag:telephones) } }
stl_gui_deficit_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:radios } } add = variable_map(stl_eff_import|flag:radios) } }
stl_gui_deficit_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fine_art } } add = variable_map(stl_eff_import|flag:fine_art) } }

# === export (stl_last_export) ===
stl_gui_export_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:grain } } add = variable_map(stl_last_export|flag:grain) } }
stl_gui_export_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fish } } add = variable_map(stl_last_export|flag:fish) } }
stl_gui_export_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fabric } } add = variable_map(stl_last_export|flag:fabric) } }
stl_gui_export_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:wood } } add = variable_map(stl_last_export|flag:wood) } }
stl_gui_export_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:groceries } } add = variable_map(stl_last_export|flag:groceries) } }
stl_gui_export_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:clothes } } add = variable_map(stl_last_export|flag:clothes) } }
stl_gui_export_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:furniture } } add = variable_map(stl_last_export|flag:furniture) } }
stl_gui_export_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:paper } } add = variable_map(stl_last_export|flag:paper) } }
stl_gui_export_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:merchant_marine } } add = variable_map(stl_last_export|flag:merchant_marine) } }
stl_gui_export_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:coal } } add = variable_map(stl_last_export|flag:coal) } }
stl_gui_export_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:iron } } add = variable_map(stl_last_export|flag:iron) } }
stl_gui_export_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:lead } } add = variable_map(stl_last_export|flag:lead) } }
stl_gui_export_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:sulfur } } add = variable_map(stl_last_export|flag:sulfur) } }
stl_gui_export_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:hardwood } } add = variable_map(stl_last_export|flag:hardwood) } }
stl_gui_export_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:rubber } } add = variable_map(stl_last_export|flag:rubber) } }
stl_gui_export_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:oil } } add = variable_map(stl_last_export|flag:oil) } }
stl_gui_export_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:silk } } add = variable_map(stl_last_export|flag:silk) } }
stl_gui_export_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:dye } } add = variable_map(stl_last_export|flag:dye) } }
stl_gui_export_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:clippers } } add = variable_map(stl_last_export|flag:clippers) } }
stl_gui_export_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:steamers } } add = variable_map(stl_last_export|flag:steamers) } }
stl_gui_export_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:glass } } add = variable_map(stl_last_export|flag:glass) } }
stl_gui_export_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fertilizer } } add = variable_map(stl_last_export|flag:fertilizer) } }
stl_gui_export_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tools } } add = variable_map(stl_last_export|flag:tools) } }
stl_gui_export_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:steel } } add = variable_map(stl_last_export|flag:steel) } }
stl_gui_export_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:engines } } add = variable_map(stl_last_export|flag:engines) } }
stl_gui_export_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:explosives } } add = variable_map(stl_last_export|flag:explosives) } }
stl_gui_export_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:ammunition } } add = variable_map(stl_last_export|flag:ammunition) } }
stl_gui_export_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:small_arms } } add = variable_map(stl_last_export|flag:small_arms) } }
stl_gui_export_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:artillery } } add = variable_map(stl_last_export|flag:artillery) } }
stl_gui_export_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tanks } } add = variable_map(stl_last_export|flag:tanks) } }
stl_gui_export_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:aeroplanes } } add = variable_map(stl_last_export|flag:aeroplanes) } }
stl_gui_export_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:meat } } add = variable_map(stl_last_export|flag:meat) } }
stl_gui_export_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fruit } } add = variable_map(stl_last_export|flag:fruit) } }
stl_gui_export_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:sugar } } add = variable_map(stl_last_export|flag:sugar) } }
stl_gui_export_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tobacco } } add = variable_map(stl_last_export|flag:tobacco) } }
stl_gui_export_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:liquor } } add = variable_map(stl_last_export|flag:liquor) } }
stl_gui_export_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:wine } } add = variable_map(stl_last_export|flag:wine) } }
stl_gui_export_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tea } } add = variable_map(stl_last_export|flag:tea) } }
stl_gui_export_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:coffee } } add = variable_map(stl_last_export|flag:coffee) } }
stl_gui_export_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:opium } } add = variable_map(stl_last_export|flag:opium) } }
stl_gui_export_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:porcelain } } add = variable_map(stl_last_export|flag:porcelain) } }
stl_gui_export_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:luxury_clothes } } add = variable_map(stl_last_export|flag:luxury_clothes) } }
stl_gui_export_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:luxury_furniture } } add = variable_map(stl_last_export|flag:luxury_furniture) } }
stl_gui_export_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:automobiles } } add = variable_map(stl_last_export|flag:automobiles) } }
stl_gui_export_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:telephones } } add = variable_map(stl_last_export|flag:telephones) } }
stl_gui_export_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:radios } } add = variable_map(stl_last_export|flag:radios) } }
stl_gui_export_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fine_art } } add = variable_map(stl_last_export|flag:fine_art) } }

# === import (stl_last_import) ===
stl_gui_import_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:grain } } add = variable_map(stl_last_import|flag:grain) } }
stl_gui_import_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fish } } add = variable_map(stl_last_import|flag:fish) } }
stl_gui_import_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fabric } } add = variable_map(stl_last_import|flag:fabric) } }
stl_gui_import_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:wood } } add = variable_map(stl_last_import|flag:wood) } }
stl_gui_import_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:groceries } } add = variable_map(stl_last_import|flag:groceries) } }
stl_gui_import_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:clothes } } add = variable_map(stl_last_import|flag:clothes) } }
stl_gui_import_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:furniture } } add = variable_map(stl_last_import|flag:furniture) } }
stl_gui_import_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:paper } } add = variable_map(stl_last_import|flag:paper) } }
stl_gui_import_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:merchant_marine } } add = variable_map(stl_last_import|flag:merchant_marine) } }
stl_gui_import_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:coal } } add = variable_map(stl_last_import|flag:coal) } }
stl_gui_import_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:iron } } add = variable_map(stl_last_import|flag:iron) } }
stl_gui_import_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:lead } } add = variable_map(stl_last_import|flag:lead) } }
stl_gui_import_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:sulfur } } add = variable_map(stl_last_import|flag:sulfur) } }
stl_gui_import_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:hardwood } } add = variable_map(stl_last_import|flag:hardwood) } }
stl_gui_import_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:rubber } } add = variable_map(stl_last_import|flag:rubber) } }
stl_gui_import_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:oil } } add = variable_map(stl_last_import|flag:oil) } }
stl_gui_import_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:silk } } add = variable_map(stl_last_import|flag:silk) } }
stl_gui_import_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:dye } } add = variable_map(stl_last_import|flag:dye) } }
stl_gui_import_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:clippers } } add = variable_map(stl_last_import|flag:clippers) } }
stl_gui_import_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:steamers } } add = variable_map(stl_last_import|flag:steamers) } }
stl_gui_import_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:glass } } add = variable_map(stl_last_import|flag:glass) } }
stl_gui_import_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fertilizer } } add = variable_map(stl_last_import|flag:fertilizer) } }
stl_gui_import_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tools } } add = variable_map(stl_last_import|flag:tools) } }
stl_gui_import_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:steel } } add = variable_map(stl_last_import|flag:steel) } }
stl_gui_import_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:engines } } add = variable_map(stl_last_import|flag:engines) } }
stl_gui_import_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:explosives } } add = variable_map(stl_last_import|flag:explosives) } }
stl_gui_import_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:ammunition } } add = variable_map(stl_last_import|flag:ammunition) } }
stl_gui_import_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:small_arms } } add = variable_map(stl_last_import|flag:small_arms) } }
stl_gui_import_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:artillery } } add = variable_map(stl_last_import|flag:artillery) } }
stl_gui_import_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tanks } } add = variable_map(stl_last_import|flag:tanks) } }
stl_gui_import_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:aeroplanes } } add = variable_map(stl_last_import|flag:aeroplanes) } }
stl_gui_import_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:meat } } add = variable_map(stl_last_import|flag:meat) } }
stl_gui_import_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fruit } } add = variable_map(stl_last_import|flag:fruit) } }
stl_gui_import_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:sugar } } add = variable_map(stl_last_import|flag:sugar) } }
stl_gui_import_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tobacco } } add = variable_map(stl_last_import|flag:tobacco) } }
stl_gui_import_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:liquor } } add = variable_map(stl_last_import|flag:liquor) } }
stl_gui_import_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:wine } } add = variable_map(stl_last_import|flag:wine) } }
stl_gui_import_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tea } } add = variable_map(stl_last_import|flag:tea) } }
stl_gui_import_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:coffee } } add = variable_map(stl_last_import|flag:coffee) } }
stl_gui_import_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:opium } } add = variable_map(stl_last_import|flag:opium) } }
stl_gui_import_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:porcelain } } add = variable_map(stl_last_import|flag:porcelain) } }
stl_gui_import_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:luxury_clothes } } add = variable_map(stl_last_import|flag:luxury_clothes) } }
stl_gui_import_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:luxury_furniture } } add = variable_map(stl_last_import|flag:luxury_furniture) } }
stl_gui_import_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:automobiles } } add = variable_map(stl_last_import|flag:automobiles) } }
stl_gui_import_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:telephones } } add = variable_map(stl_last_import|flag:telephones) } }
stl_gui_import_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:radios } } add = variable_map(stl_last_import|flag:radios) } }
stl_gui_import_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fine_art } } add = variable_map(stl_last_import|flag:fine_art) } }

# === transport (stl_transport) ===
stl_gui_transport_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:grain } } add = variable_map(stl_transport|flag:grain) } }
stl_gui_transport_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:fish } } add = variable_map(stl_transport|flag:fish) } }
stl_gui_transport_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:fabric } } add = variable_map(stl_transport|flag:fabric) } }
stl_gui_transport_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:wood } } add = variable_map(stl_transport|flag:wood) } }
stl_gui_transport_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:groceries } } add = variable_map(stl_transport|flag:groceries) } }
stl_gui_transport_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:clothes } } add = variable_map(stl_transport|flag:clothes) } }
stl_gui_transport_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:furniture } } add = variable_map(stl_transport|flag:furniture) } }
stl_gui_transport_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:paper } } add = variable_map(stl_transport|flag:paper) } }
stl_gui_transport_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:merchant_marine } } add = variable_map(stl_transport|flag:merchant_marine) } }
stl_gui_transport_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:coal } } add = variable_map(stl_transport|flag:coal) } }
stl_gui_transport_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:iron } } add = variable_map(stl_transport|flag:iron) } }
stl_gui_transport_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:lead } } add = variable_map(stl_transport|flag:lead) } }
stl_gui_transport_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:sulfur } } add = variable_map(stl_transport|flag:sulfur) } }
stl_gui_transport_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:hardwood } } add = variable_map(stl_transport|flag:hardwood) } }
stl_gui_transport_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:rubber } } add = variable_map(stl_transport|flag:rubber) } }
stl_gui_transport_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:oil } } add = variable_map(stl_transport|flag:oil) } }
stl_gui_transport_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:silk } } add = variable_map(stl_transport|flag:silk) } }
stl_gui_transport_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:dye } } add = variable_map(stl_transport|flag:dye) } }
stl_gui_transport_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:clippers } } add = variable_map(stl_transport|flag:clippers) } }
stl_gui_transport_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:steamers } } add = variable_map(stl_transport|flag:steamers) } }
stl_gui_transport_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:glass } } add = variable_map(stl_transport|flag:glass) } }
stl_gui_transport_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:fertilizer } } add = variable_map(stl_transport|flag:fertilizer) } }
stl_gui_transport_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:tools } } add = variable_map(stl_transport|flag:tools) } }
stl_gui_transport_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:steel } } add = variable_map(stl_transport|flag:steel) } }
stl_gui_transport_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:engines } } add = variable_map(stl_transport|flag:engines) } }
stl_gui_transport_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:explosives } } add = variable_map(stl_transport|flag:explosives) } }
stl_gui_transport_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:ammunition } } add = variable_map(stl_transport|flag:ammunition) } }
stl_gui_transport_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:small_arms } } add = variable_map(stl_transport|flag:small_arms) } }
stl_gui_transport_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:artillery } } add = variable_map(stl_transport|flag:artillery) } }
stl_gui_transport_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:tanks } } add = variable_map(stl_transport|flag:tanks) } }
stl_gui_transport_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:aeroplanes } } add = variable_map(stl_transport|flag:aeroplanes) } }
stl_gui_transport_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:meat } } add = variable_map(stl_transport|flag:meat) } }
stl_gui_transport_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:fruit } } add = variable_map(stl_transport|flag:fruit) } }
stl_gui_transport_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:sugar } } add = variable_map(stl_transport|flag:sugar) } }
stl_gui_transport_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:tobacco } } add = variable_map(stl_transport|flag:tobacco) } }
stl_gui_transport_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:liquor } } add = variable_map(stl_transport|flag:liquor) } }
stl_gui_transport_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:wine } } add = variable_map(stl_transport|flag:wine) } }
stl_gui_transport_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:tea } } add = variable_map(stl_transport|flag:tea) } }
stl_gui_transport_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:coffee } } add = variable_map(stl_transport|flag:coffee) } }
stl_gui_transport_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:opium } } add = variable_map(stl_transport|flag:opium) } }
stl_gui_transport_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:porcelain } } add = variable_map(stl_transport|flag:porcelain) } }
stl_gui_transport_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:luxury_clothes } } add = variable_map(stl_transport|flag:luxury_clothes) } }
stl_gui_transport_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:luxury_furniture } } add = variable_map(stl_transport|flag:luxury_furniture) } }
stl_gui_transport_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:automobiles } } add = variable_map(stl_transport|flag:automobiles) } }
stl_gui_transport_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:telephones } } add = variable_map(stl_transport|flag:telephones) } }
stl_gui_transport_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:radios } } add = variable_map(stl_transport|flag:radios) } }
stl_gui_transport_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_transport key = flag:fine_art } } add = variable_map(stl_transport|flag:fine_art) } }

# === access_export (stl_access_export) ===
stl_gui_access_export_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:grain } } add = variable_map(stl_access_export|flag:grain) } }
stl_gui_access_export_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fish } } add = variable_map(stl_access_export|flag:fish) } }
stl_gui_access_export_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fabric } } add = variable_map(stl_access_export|flag:fabric) } }
stl_gui_access_export_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:wood } } add = variable_map(stl_access_export|flag:wood) } }
stl_gui_access_export_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:groceries } } add = variable_map(stl_access_export|flag:groceries) } }
stl_gui_access_export_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:clothes } } add = variable_map(stl_access_export|flag:clothes) } }
stl_gui_access_export_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:furniture } } add = variable_map(stl_access_export|flag:furniture) } }
stl_gui_access_export_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:paper } } add = variable_map(stl_access_export|flag:paper) } }
stl_gui_access_export_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:merchant_marine } } add = variable_map(stl_access_export|flag:merchant_marine) } }
stl_gui_access_export_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:coal } } add = variable_map(stl_access_export|flag:coal) } }
stl_gui_access_export_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:iron } } add = variable_map(stl_access_export|flag:iron) } }
stl_gui_access_export_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:lead } } add = variable_map(stl_access_export|flag:lead) } }
stl_gui_access_export_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:sulfur } } add = variable_map(stl_access_export|flag:sulfur) } }
stl_gui_access_export_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:hardwood } } add = variable_map(stl_access_export|flag:hardwood) } }
stl_gui_access_export_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:rubber } } add = variable_map(stl_access_export|flag:rubber) } }
stl_gui_access_export_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:oil } } add = variable_map(stl_access_export|flag:oil) } }
stl_gui_access_export_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:silk } } add = variable_map(stl_access_export|flag:silk) } }
stl_gui_access_export_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:dye } } add = variable_map(stl_access_export|flag:dye) } }
stl_gui_access_export_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:clippers } } add = variable_map(stl_access_export|flag:clippers) } }
stl_gui_access_export_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:steamers } } add = variable_map(stl_access_export|flag:steamers) } }
stl_gui_access_export_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:glass } } add = variable_map(stl_access_export|flag:glass) } }
stl_gui_access_export_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fertilizer } } add = variable_map(stl_access_export|flag:fertilizer) } }
stl_gui_access_export_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tools } } add = variable_map(stl_access_export|flag:tools) } }
stl_gui_access_export_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:steel } } add = variable_map(stl_access_export|flag:steel) } }
stl_gui_access_export_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:engines } } add = variable_map(stl_access_export|flag:engines) } }
stl_gui_access_export_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:explosives } } add = variable_map(stl_access_export|flag:explosives) } }
stl_gui_access_export_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:ammunition } } add = variable_map(stl_access_export|flag:ammunition) } }
stl_gui_access_export_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:small_arms } } add = variable_map(stl_access_export|flag:small_arms) } }
stl_gui_access_export_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:artillery } } add = variable_map(stl_access_export|flag:artillery) } }
stl_gui_access_export_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tanks } } add = variable_map(stl_access_export|flag:tanks) } }
stl_gui_access_export_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:aeroplanes } } add = variable_map(stl_access_export|flag:aeroplanes) } }
stl_gui_access_export_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:meat } } add = variable_map(stl_access_export|flag:meat) } }
stl_gui_access_export_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fruit } } add = variable_map(stl_access_export|flag:fruit) } }
stl_gui_access_export_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:sugar } } add = variable_map(stl_access_export|flag:sugar) } }
stl_gui_access_export_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tobacco } } add = variable_map(stl_access_export|flag:tobacco) } }
stl_gui_access_export_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:liquor } } add = variable_map(stl_access_export|flag:liquor) } }
stl_gui_access_export_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:wine } } add = variable_map(stl_access_export|flag:wine) } }
stl_gui_access_export_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tea } } add = variable_map(stl_access_export|flag:tea) } }
stl_gui_access_export_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:coffee } } add = variable_map(stl_access_export|flag:coffee) } }
stl_gui_access_export_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:opium } } add = variable_map(stl_access_export|flag:opium) } }
stl_gui_access_export_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:porcelain } } add = variable_map(stl_access_export|flag:porcelain) } }
stl_gui_access_export_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:luxury_clothes } } add = variable_map(stl_access_export|flag:luxury_clothes) } }
stl_gui_access_export_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:luxury_furniture } } add = variable_map(stl_access_export|flag:luxury_furniture) } }
stl_gui_access_export_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:automobiles } } add = variable_map(stl_access_export|flag:automobiles) } }
stl_gui_access_export_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:telephones } } add = variable_map(stl_access_export|flag:telephones) } }
stl_gui_access_export_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:radios } } add = variable_map(stl_access_export|flag:radios) } }
stl_gui_access_export_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fine_art } } add = variable_map(stl_access_export|flag:fine_art) } }

# === access_import (stl_access_import) ===
stl_gui_access_import_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:grain } } add = variable_map(stl_access_import|flag:grain) } }
stl_gui_access_import_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fish } } add = variable_map(stl_access_import|flag:fish) } }
stl_gui_access_import_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fabric } } add = variable_map(stl_access_import|flag:fabric) } }
stl_gui_access_import_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:wood } } add = variable_map(stl_access_import|flag:wood) } }
stl_gui_access_import_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:groceries } } add = variable_map(stl_access_import|flag:groceries) } }
stl_gui_access_import_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:clothes } } add = variable_map(stl_access_import|flag:clothes) } }
stl_gui_access_import_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:furniture } } add = variable_map(stl_access_import|flag:furniture) } }
stl_gui_access_import_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:paper } } add = variable_map(stl_access_import|flag:paper) } }
stl_gui_access_import_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:merchant_marine } } add = variable_map(stl_access_import|flag:merchant_marine) } }
stl_gui_access_import_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:coal } } add = variable_map(stl_access_import|flag:coal) } }
stl_gui_access_import_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:iron } } add = variable_map(stl_access_import|flag:iron) } }
stl_gui_access_import_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:lead } } add = variable_map(stl_access_import|flag:lead) } }
stl_gui_access_import_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:sulfur } } add = variable_map(stl_access_import|flag:sulfur) } }
stl_gui_access_import_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:hardwood } } add = variable_map(stl_access_import|flag:hardwood) } }
stl_gui_access_import_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:rubber } } add = variable_map(stl_access_import|flag:rubber) } }
stl_gui_access_import_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:oil } } add = variable_map(stl_access_import|flag:oil) } }
stl_gui_access_import_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:silk } } add = variable_map(stl_access_import|flag:silk) } }
stl_gui_access_import_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:dye } } add = variable_map(stl_access_import|flag:dye) } }
stl_gui_access_import_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:clippers } } add = variable_map(stl_access_import|flag:clippers) } }
stl_gui_access_import_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:steamers } } add = variable_map(stl_access_import|flag:steamers) } }
stl_gui_access_import_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:glass } } add = variable_map(stl_access_import|flag:glass) } }
stl_gui_access_import_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fertilizer } } add = variable_map(stl_access_import|flag:fertilizer) } }
stl_gui_access_import_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tools } } add = variable_map(stl_access_import|flag:tools) } }
stl_gui_access_import_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:steel } } add = variable_map(stl_access_import|flag:steel) } }
stl_gui_access_import_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:engines } } add = variable_map(stl_access_import|flag:engines) } }
stl_gui_access_import_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:explosives } } add = variable_map(stl_access_import|flag:explosives) } }
stl_gui_access_import_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:ammunition } } add = variable_map(stl_access_import|flag:ammunition) } }
stl_gui_access_import_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:small_arms } } add = variable_map(stl_access_import|flag:small_arms) } }
stl_gui_access_import_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:artillery } } add = variable_map(stl_access_import|flag:artillery) } }
stl_gui_access_import_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tanks } } add = variable_map(stl_access_import|flag:tanks) } }
stl_gui_access_import_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:aeroplanes } } add = variable_map(stl_access_import|flag:aeroplanes) } }
stl_gui_access_import_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:meat } } add = variable_map(stl_access_import|flag:meat) } }
stl_gui_access_import_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fruit } } add = variable_map(stl_access_import|flag:fruit) } }
stl_gui_access_import_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:sugar } } add = variable_map(stl_access_import|flag:sugar) } }
stl_gui_access_import_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tobacco } } add = variable_map(stl_access_import|flag:tobacco) } }
stl_gui_access_import_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:liquor } } add = variable_map(stl_access_import|flag:liquor) } }
stl_gui_access_import_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:wine } } add = variable_map(stl_access_import|flag:wine) } }
stl_gui_access_import_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tea } } add = variable_map(stl_access_import|flag:tea) } }
stl_gui_access_import_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:coffee } } add = variable_map(stl_access_import|flag:coffee) } }
stl_gui_access_import_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:opium } } add = variable_map(stl_access_import|flag:opium) } }
stl_gui_access_import_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:porcelain } } add = variable_map(stl_access_import|flag:porcelain) } }
stl_gui_access_import_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:luxury_clothes } } add = variable_map(stl_access_import|flag:luxury_clothes) } }
stl_gui_access_import_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:luxury_furniture } } add = variable_map(stl_access_import|flag:luxury_furniture) } }
stl_gui_access_import_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:automobiles } } add = variable_map(stl_access_import|flag:automobiles) } }
stl_gui_access_import_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:telephones } } add = variable_map(stl_access_import|flag:telephones) } }
stl_gui_access_import_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:radios } } add = variable_map(stl_access_import|flag:radios) } }
stl_gui_access_import_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fine_art } } add = variable_map(stl_access_import|flag:fine_art) } }

# === market_price (stl_market_price) ===
stl_gui_market_price_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:grain } } add = variable_map(stl_market_price|flag:grain) } }
stl_gui_market_price_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:fish } } add = variable_map(stl_market_price|flag:fish) } }
stl_gui_market_price_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:fabric } } add = variable_map(stl_market_price|flag:fabric) } }
stl_gui_market_price_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:wood } } add = variable_map(stl_market_price|flag:wood) } }
stl_gui_market_price_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:groceries } } add = variable_map(stl_market_price|flag:groceries) } }
stl_gui_market_price_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:clothes } } add = variable_map(stl_market_price|flag:clothes) } }
stl_gui_market_price_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:furniture } } add = variable_map(stl_market_price|flag:furniture) } }
stl_gui_market_price_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:paper } } add = variable_map(stl_market_price|flag:paper) } }
stl_gui_market_price_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:merchant_marine } } add = variable_map(stl_market_price|flag:merchant_marine) } }
stl_gui_market_price_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:coal } } add = variable_map(stl_market_price|flag:coal) } }
stl_gui_market_price_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:iron } } add = variable_map(stl_market_price|flag:iron) } }
stl_gui_market_price_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:lead } } add = variable_map(stl_market_price|flag:lead) } }
stl_gui_market_price_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:sulfur } } add = variable_map(stl_market_price|flag:sulfur) } }
stl_gui_market_price_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:hardwood } } add = variable_map(stl_market_price|flag:hardwood) } }
stl_gui_market_price_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:rubber } } add = variable_map(stl_market_price|flag:rubber) } }
stl_gui_market_price_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:oil } } add = variable_map(stl_market_price|flag:oil) } }
stl_gui_market_price_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:silk } } add = variable_map(stl_market_price|flag:silk) } }
stl_gui_market_price_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:dye } } add = variable_map(stl_market_price|flag:dye) } }
stl_gui_market_price_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:clippers } } add = variable_map(stl_market_price|flag:clippers) } }
stl_gui_market_price_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:steamers } } add = variable_map(stl_market_price|flag:steamers) } }
stl_gui_market_price_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:glass } } add = variable_map(stl_market_price|flag:glass) } }
stl_gui_market_price_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:fertilizer } } add = variable_map(stl_market_price|flag:fertilizer) } }
stl_gui_market_price_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:tools } } add = variable_map(stl_market_price|flag:tools) } }
stl_gui_market_price_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:steel } } add = variable_map(stl_market_price|flag:steel) } }
stl_gui_market_price_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:engines } } add = variable_map(stl_market_price|flag:engines) } }
stl_gui_market_price_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:explosives } } add = variable_map(stl_market_price|flag:explosives) } }
stl_gui_market_price_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:ammunition } } add = variable_map(stl_market_price|flag:ammunition) } }
stl_gui_market_price_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:small_arms } } add = variable_map(stl_market_price|flag:small_arms) } }
stl_gui_market_price_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:artillery } } add = variable_map(stl_market_price|flag:artillery) } }
stl_gui_market_price_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:tanks } } add = variable_map(stl_market_price|flag:tanks) } }
stl_gui_market_price_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:aeroplanes } } add = variable_map(stl_market_price|flag:aeroplanes) } }
stl_gui_market_price_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:meat } } add = variable_map(stl_market_price|flag:meat) } }
stl_gui_market_price_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:fruit } } add = variable_map(stl_market_price|flag:fruit) } }
stl_gui_market_price_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:sugar } } add = variable_map(stl_market_price|flag:sugar) } }
stl_gui_market_price_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:tobacco } } add = variable_map(stl_market_price|flag:tobacco) } }
stl_gui_market_price_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:liquor } } add = variable_map(stl_market_price|flag:liquor) } }
stl_gui_market_price_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:wine } } add = variable_map(stl_market_price|flag:wine) } }
stl_gui_market_price_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:tea } } add = variable_map(stl_market_price|flag:tea) } }
stl_gui_market_price_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:coffee } } add = variable_map(stl_market_price|flag:coffee) } }
stl_gui_market_price_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:opium } } add = variable_map(stl_market_price|flag:opium) } }
stl_gui_market_price_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:porcelain } } add = variable_map(stl_market_price|flag:porcelain) } }
stl_gui_market_price_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:luxury_clothes } } add = variable_map(stl_market_price|flag:luxury_clothes) } }
stl_gui_market_price_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:luxury_furniture } } add = variable_map(stl_market_price|flag:luxury_furniture) } }
stl_gui_market_price_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:automobiles } } add = variable_map(stl_market_price|flag:automobiles) } }
stl_gui_market_price_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:telephones } } add = variable_map(stl_market_price|flag:telephones) } }
stl_gui_market_price_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:radios } } add = variable_map(stl_market_price|flag:radios) } }
stl_gui_market_price_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_market_price key = flag:fine_art } } add = variable_map(stl_market_price|flag:fine_art) } }

# === local_price (stl_price) ===
stl_gui_local_price_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:grain } } add = variable_map(stl_price|flag:grain) } }
stl_gui_local_price_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:fish } } add = variable_map(stl_price|flag:fish) } }
stl_gui_local_price_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:fabric } } add = variable_map(stl_price|flag:fabric) } }
stl_gui_local_price_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:wood } } add = variable_map(stl_price|flag:wood) } }
stl_gui_local_price_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:groceries } } add = variable_map(stl_price|flag:groceries) } }
stl_gui_local_price_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:clothes } } add = variable_map(stl_price|flag:clothes) } }
stl_gui_local_price_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:furniture } } add = variable_map(stl_price|flag:furniture) } }
stl_gui_local_price_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:paper } } add = variable_map(stl_price|flag:paper) } }
stl_gui_local_price_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:merchant_marine } } add = variable_map(stl_price|flag:merchant_marine) } }
stl_gui_local_price_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:coal } } add = variable_map(stl_price|flag:coal) } }
stl_gui_local_price_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:iron } } add = variable_map(stl_price|flag:iron) } }
stl_gui_local_price_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:lead } } add = variable_map(stl_price|flag:lead) } }
stl_gui_local_price_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:sulfur } } add = variable_map(stl_price|flag:sulfur) } }
stl_gui_local_price_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:hardwood } } add = variable_map(stl_price|flag:hardwood) } }
stl_gui_local_price_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:rubber } } add = variable_map(stl_price|flag:rubber) } }
stl_gui_local_price_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:oil } } add = variable_map(stl_price|flag:oil) } }
stl_gui_local_price_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:silk } } add = variable_map(stl_price|flag:silk) } }
stl_gui_local_price_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:dye } } add = variable_map(stl_price|flag:dye) } }
stl_gui_local_price_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:clippers } } add = variable_map(stl_price|flag:clippers) } }
stl_gui_local_price_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:steamers } } add = variable_map(stl_price|flag:steamers) } }
stl_gui_local_price_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:glass } } add = variable_map(stl_price|flag:glass) } }
stl_gui_local_price_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:fertilizer } } add = variable_map(stl_price|flag:fertilizer) } }
stl_gui_local_price_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:tools } } add = variable_map(stl_price|flag:tools) } }
stl_gui_local_price_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:steel } } add = variable_map(stl_price|flag:steel) } }
stl_gui_local_price_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:engines } } add = variable_map(stl_price|flag:engines) } }
stl_gui_local_price_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:explosives } } add = variable_map(stl_price|flag:explosives) } }
stl_gui_local_price_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:ammunition } } add = variable_map(stl_price|flag:ammunition) } }
stl_gui_local_price_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:small_arms } } add = variable_map(stl_price|flag:small_arms) } }
stl_gui_local_price_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:artillery } } add = variable_map(stl_price|flag:artillery) } }
stl_gui_local_price_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:tanks } } add = variable_map(stl_price|flag:tanks) } }
stl_gui_local_price_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:aeroplanes } } add = variable_map(stl_price|flag:aeroplanes) } }
stl_gui_local_price_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:meat } } add = variable_map(stl_price|flag:meat) } }
stl_gui_local_price_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:fruit } } add = variable_map(stl_price|flag:fruit) } }
stl_gui_local_price_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:sugar } } add = variable_map(stl_price|flag:sugar) } }
stl_gui_local_price_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:tobacco } } add = variable_map(stl_price|flag:tobacco) } }
stl_gui_local_price_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:liquor } } add = variable_map(stl_price|flag:liquor) } }
stl_gui_local_price_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:wine } } add = variable_map(stl_price|flag:wine) } }
stl_gui_local_price_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:tea } } add = variable_map(stl_price|flag:tea) } }
stl_gui_local_price_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:coffee } } add = variable_map(stl_price|flag:coffee) } }
stl_gui_local_price_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:opium } } add = variable_map(stl_price|flag:opium) } }
stl_gui_local_price_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:porcelain } } add = variable_map(stl_price|flag:porcelain) } }
stl_gui_local_price_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:luxury_clothes } } add = variable_map(stl_price|flag:luxury_clothes) } }
stl_gui_local_price_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:luxury_furniture } } add = variable_map(stl_price|flag:luxury_furniture) } }
stl_gui_local_price_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:automobiles } } add = variable_map(stl_price|flag:automobiles) } }
stl_gui_local_price_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:telephones } } add = variable_map(stl_price|flag:telephones) } }
stl_gui_local_price_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:radios } } add = variable_map(stl_price|flag:radios) } }
stl_gui_local_price_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_price key = flag:fine_art } } add = variable_map(stl_price|flag:fine_art) } }

# === trade (last_export - last_import, signed) ===
stl_gui_trade_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:grain } } add = variable_map(stl_last_export|flag:grain) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:grain } } subtract = variable_map(stl_last_import|flag:grain) } }
stl_gui_trade_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fish } } add = variable_map(stl_last_export|flag:fish) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fish } } subtract = variable_map(stl_last_import|flag:fish) } }
stl_gui_trade_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fabric } } add = variable_map(stl_last_export|flag:fabric) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fabric } } subtract = variable_map(stl_last_import|flag:fabric) } }
stl_gui_trade_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:wood } } add = variable_map(stl_last_export|flag:wood) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:wood } } subtract = variable_map(stl_last_import|flag:wood) } }
stl_gui_trade_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:groceries } } add = variable_map(stl_last_export|flag:groceries) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:groceries } } subtract = variable_map(stl_last_import|flag:groceries) } }
stl_gui_trade_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:clothes } } add = variable_map(stl_last_export|flag:clothes) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:clothes } } subtract = variable_map(stl_last_import|flag:clothes) } }
stl_gui_trade_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:furniture } } add = variable_map(stl_last_export|flag:furniture) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:furniture } } subtract = variable_map(stl_last_import|flag:furniture) } }
stl_gui_trade_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:paper } } add = variable_map(stl_last_export|flag:paper) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:paper } } subtract = variable_map(stl_last_import|flag:paper) } }
stl_gui_trade_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:merchant_marine } } add = variable_map(stl_last_export|flag:merchant_marine) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:merchant_marine } } subtract = variable_map(stl_last_import|flag:merchant_marine) } }
stl_gui_trade_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:coal } } add = variable_map(stl_last_export|flag:coal) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:coal } } subtract = variable_map(stl_last_import|flag:coal) } }
stl_gui_trade_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:iron } } add = variable_map(stl_last_export|flag:iron) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:iron } } subtract = variable_map(stl_last_import|flag:iron) } }
stl_gui_trade_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:lead } } add = variable_map(stl_last_export|flag:lead) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:lead } } subtract = variable_map(stl_last_import|flag:lead) } }
stl_gui_trade_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:sulfur } } add = variable_map(stl_last_export|flag:sulfur) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:sulfur } } subtract = variable_map(stl_last_import|flag:sulfur) } }
stl_gui_trade_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:hardwood } } add = variable_map(stl_last_export|flag:hardwood) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:hardwood } } subtract = variable_map(stl_last_import|flag:hardwood) } }
stl_gui_trade_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:rubber } } add = variable_map(stl_last_export|flag:rubber) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:rubber } } subtract = variable_map(stl_last_import|flag:rubber) } }
stl_gui_trade_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:oil } } add = variable_map(stl_last_export|flag:oil) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:oil } } subtract = variable_map(stl_last_import|flag:oil) } }
stl_gui_trade_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:silk } } add = variable_map(stl_last_export|flag:silk) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:silk } } subtract = variable_map(stl_last_import|flag:silk) } }
stl_gui_trade_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:dye } } add = variable_map(stl_last_export|flag:dye) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:dye } } subtract = variable_map(stl_last_import|flag:dye) } }
stl_gui_trade_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:clippers } } add = variable_map(stl_last_export|flag:clippers) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:clippers } } subtract = variable_map(stl_last_import|flag:clippers) } }
stl_gui_trade_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:steamers } } add = variable_map(stl_last_export|flag:steamers) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:steamers } } subtract = variable_map(stl_last_import|flag:steamers) } }
stl_gui_trade_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:glass } } add = variable_map(stl_last_export|flag:glass) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:glass } } subtract = variable_map(stl_last_import|flag:glass) } }
stl_gui_trade_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fertilizer } } add = variable_map(stl_last_export|flag:fertilizer) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fertilizer } } subtract = variable_map(stl_last_import|flag:fertilizer) } }
stl_gui_trade_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tools } } add = variable_map(stl_last_export|flag:tools) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tools } } subtract = variable_map(stl_last_import|flag:tools) } }
stl_gui_trade_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:steel } } add = variable_map(stl_last_export|flag:steel) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:steel } } subtract = variable_map(stl_last_import|flag:steel) } }
stl_gui_trade_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:engines } } add = variable_map(stl_last_export|flag:engines) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:engines } } subtract = variable_map(stl_last_import|flag:engines) } }
stl_gui_trade_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:explosives } } add = variable_map(stl_last_export|flag:explosives) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:explosives } } subtract = variable_map(stl_last_import|flag:explosives) } }
stl_gui_trade_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:ammunition } } add = variable_map(stl_last_export|flag:ammunition) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:ammunition } } subtract = variable_map(stl_last_import|flag:ammunition) } }
stl_gui_trade_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:small_arms } } add = variable_map(stl_last_export|flag:small_arms) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:small_arms } } subtract = variable_map(stl_last_import|flag:small_arms) } }
stl_gui_trade_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:artillery } } add = variable_map(stl_last_export|flag:artillery) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:artillery } } subtract = variable_map(stl_last_import|flag:artillery) } }
stl_gui_trade_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tanks } } add = variable_map(stl_last_export|flag:tanks) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tanks } } subtract = variable_map(stl_last_import|flag:tanks) } }
stl_gui_trade_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:aeroplanes } } add = variable_map(stl_last_export|flag:aeroplanes) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:aeroplanes } } subtract = variable_map(stl_last_import|flag:aeroplanes) } }
stl_gui_trade_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:meat } } add = variable_map(stl_last_export|flag:meat) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:meat } } subtract = variable_map(stl_last_import|flag:meat) } }
stl_gui_trade_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fruit } } add = variable_map(stl_last_export|flag:fruit) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fruit } } subtract = variable_map(stl_last_import|flag:fruit) } }
stl_gui_trade_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:sugar } } add = variable_map(stl_last_export|flag:sugar) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:sugar } } subtract = variable_map(stl_last_import|flag:sugar) } }
stl_gui_trade_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tobacco } } add = variable_map(stl_last_export|flag:tobacco) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tobacco } } subtract = variable_map(stl_last_import|flag:tobacco) } }
stl_gui_trade_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:liquor } } add = variable_map(stl_last_export|flag:liquor) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:liquor } } subtract = variable_map(stl_last_import|flag:liquor) } }
stl_gui_trade_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:wine } } add = variable_map(stl_last_export|flag:wine) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:wine } } subtract = variable_map(stl_last_import|flag:wine) } }
stl_gui_trade_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:tea } } add = variable_map(stl_last_export|flag:tea) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:tea } } subtract = variable_map(stl_last_import|flag:tea) } }
stl_gui_trade_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:coffee } } add = variable_map(stl_last_export|flag:coffee) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:coffee } } subtract = variable_map(stl_last_import|flag:coffee) } }
stl_gui_trade_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:opium } } add = variable_map(stl_last_export|flag:opium) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:opium } } subtract = variable_map(stl_last_import|flag:opium) } }
stl_gui_trade_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:porcelain } } add = variable_map(stl_last_export|flag:porcelain) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:porcelain } } subtract = variable_map(stl_last_import|flag:porcelain) } }
stl_gui_trade_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:luxury_clothes } } add = variable_map(stl_last_export|flag:luxury_clothes) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:luxury_clothes } } subtract = variable_map(stl_last_import|flag:luxury_clothes) } }
stl_gui_trade_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:luxury_furniture } } add = variable_map(stl_last_export|flag:luxury_furniture) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:luxury_furniture } } subtract = variable_map(stl_last_import|flag:luxury_furniture) } }
stl_gui_trade_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:automobiles } } add = variable_map(stl_last_export|flag:automobiles) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:automobiles } } subtract = variable_map(stl_last_import|flag:automobiles) } }
stl_gui_trade_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:telephones } } add = variable_map(stl_last_export|flag:telephones) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:telephones } } subtract = variable_map(stl_last_import|flag:telephones) } }
stl_gui_trade_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:radios } } add = variable_map(stl_last_export|flag:radios) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:radios } } subtract = variable_map(stl_last_import|flag:radios) } }
stl_gui_trade_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_last_export key = flag:fine_art } } add = variable_map(stl_last_export|flag:fine_art) } if = { limit = { is_key_in_variable_map = { name = stl_last_import key = flag:fine_art } } subtract = variable_map(stl_last_import|flag:fine_art) } }

# === target (eff_export - eff_import, signed) ===
stl_gui_target_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:grain } } add = variable_map(stl_eff_export|flag:grain) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:grain } } subtract = variable_map(stl_eff_import|flag:grain) } }
stl_gui_target_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fish } } add = variable_map(stl_eff_export|flag:fish) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fish } } subtract = variable_map(stl_eff_import|flag:fish) } }
stl_gui_target_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fabric } } add = variable_map(stl_eff_export|flag:fabric) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fabric } } subtract = variable_map(stl_eff_import|flag:fabric) } }
stl_gui_target_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:wood } } add = variable_map(stl_eff_export|flag:wood) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:wood } } subtract = variable_map(stl_eff_import|flag:wood) } }
stl_gui_target_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:groceries } } add = variable_map(stl_eff_export|flag:groceries) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:groceries } } subtract = variable_map(stl_eff_import|flag:groceries) } }
stl_gui_target_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:clothes } } add = variable_map(stl_eff_export|flag:clothes) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:clothes } } subtract = variable_map(stl_eff_import|flag:clothes) } }
stl_gui_target_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:furniture } } add = variable_map(stl_eff_export|flag:furniture) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:furniture } } subtract = variable_map(stl_eff_import|flag:furniture) } }
stl_gui_target_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:paper } } add = variable_map(stl_eff_export|flag:paper) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:paper } } subtract = variable_map(stl_eff_import|flag:paper) } }
stl_gui_target_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:merchant_marine } } add = variable_map(stl_eff_export|flag:merchant_marine) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:merchant_marine } } subtract = variable_map(stl_eff_import|flag:merchant_marine) } }
stl_gui_target_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:coal } } add = variable_map(stl_eff_export|flag:coal) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:coal } } subtract = variable_map(stl_eff_import|flag:coal) } }
stl_gui_target_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:iron } } add = variable_map(stl_eff_export|flag:iron) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:iron } } subtract = variable_map(stl_eff_import|flag:iron) } }
stl_gui_target_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:lead } } add = variable_map(stl_eff_export|flag:lead) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:lead } } subtract = variable_map(stl_eff_import|flag:lead) } }
stl_gui_target_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:sulfur } } add = variable_map(stl_eff_export|flag:sulfur) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:sulfur } } subtract = variable_map(stl_eff_import|flag:sulfur) } }
stl_gui_target_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:hardwood } } add = variable_map(stl_eff_export|flag:hardwood) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:hardwood } } subtract = variable_map(stl_eff_import|flag:hardwood) } }
stl_gui_target_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:rubber } } add = variable_map(stl_eff_export|flag:rubber) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:rubber } } subtract = variable_map(stl_eff_import|flag:rubber) } }
stl_gui_target_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:oil } } add = variable_map(stl_eff_export|flag:oil) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:oil } } subtract = variable_map(stl_eff_import|flag:oil) } }
stl_gui_target_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:silk } } add = variable_map(stl_eff_export|flag:silk) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:silk } } subtract = variable_map(stl_eff_import|flag:silk) } }
stl_gui_target_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:dye } } add = variable_map(stl_eff_export|flag:dye) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:dye } } subtract = variable_map(stl_eff_import|flag:dye) } }
stl_gui_target_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:clippers } } add = variable_map(stl_eff_export|flag:clippers) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:clippers } } subtract = variable_map(stl_eff_import|flag:clippers) } }
stl_gui_target_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:steamers } } add = variable_map(stl_eff_export|flag:steamers) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:steamers } } subtract = variable_map(stl_eff_import|flag:steamers) } }
stl_gui_target_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:glass } } add = variable_map(stl_eff_export|flag:glass) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:glass } } subtract = variable_map(stl_eff_import|flag:glass) } }
stl_gui_target_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fertilizer } } add = variable_map(stl_eff_export|flag:fertilizer) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fertilizer } } subtract = variable_map(stl_eff_import|flag:fertilizer) } }
stl_gui_target_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tools } } add = variable_map(stl_eff_export|flag:tools) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tools } } subtract = variable_map(stl_eff_import|flag:tools) } }
stl_gui_target_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:steel } } add = variable_map(stl_eff_export|flag:steel) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:steel } } subtract = variable_map(stl_eff_import|flag:steel) } }
stl_gui_target_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:engines } } add = variable_map(stl_eff_export|flag:engines) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:engines } } subtract = variable_map(stl_eff_import|flag:engines) } }
stl_gui_target_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:explosives } } add = variable_map(stl_eff_export|flag:explosives) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:explosives } } subtract = variable_map(stl_eff_import|flag:explosives) } }
stl_gui_target_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:ammunition } } add = variable_map(stl_eff_export|flag:ammunition) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:ammunition } } subtract = variable_map(stl_eff_import|flag:ammunition) } }
stl_gui_target_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:small_arms } } add = variable_map(stl_eff_export|flag:small_arms) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:small_arms } } subtract = variable_map(stl_eff_import|flag:small_arms) } }
stl_gui_target_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:artillery } } add = variable_map(stl_eff_export|flag:artillery) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:artillery } } subtract = variable_map(stl_eff_import|flag:artillery) } }
stl_gui_target_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tanks } } add = variable_map(stl_eff_export|flag:tanks) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tanks } } subtract = variable_map(stl_eff_import|flag:tanks) } }
stl_gui_target_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:aeroplanes } } add = variable_map(stl_eff_export|flag:aeroplanes) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:aeroplanes } } subtract = variable_map(stl_eff_import|flag:aeroplanes) } }
stl_gui_target_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:meat } } add = variable_map(stl_eff_export|flag:meat) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:meat } } subtract = variable_map(stl_eff_import|flag:meat) } }
stl_gui_target_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fruit } } add = variable_map(stl_eff_export|flag:fruit) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fruit } } subtract = variable_map(stl_eff_import|flag:fruit) } }
stl_gui_target_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:sugar } } add = variable_map(stl_eff_export|flag:sugar) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:sugar } } subtract = variable_map(stl_eff_import|flag:sugar) } }
stl_gui_target_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tobacco } } add = variable_map(stl_eff_export|flag:tobacco) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tobacco } } subtract = variable_map(stl_eff_import|flag:tobacco) } }
stl_gui_target_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:liquor } } add = variable_map(stl_eff_export|flag:liquor) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:liquor } } subtract = variable_map(stl_eff_import|flag:liquor) } }
stl_gui_target_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:wine } } add = variable_map(stl_eff_export|flag:wine) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:wine } } subtract = variable_map(stl_eff_import|flag:wine) } }
stl_gui_target_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:tea } } add = variable_map(stl_eff_export|flag:tea) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:tea } } subtract = variable_map(stl_eff_import|flag:tea) } }
stl_gui_target_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:coffee } } add = variable_map(stl_eff_export|flag:coffee) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:coffee } } subtract = variable_map(stl_eff_import|flag:coffee) } }
stl_gui_target_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:opium } } add = variable_map(stl_eff_export|flag:opium) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:opium } } subtract = variable_map(stl_eff_import|flag:opium) } }
stl_gui_target_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:porcelain } } add = variable_map(stl_eff_export|flag:porcelain) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:porcelain } } subtract = variable_map(stl_eff_import|flag:porcelain) } }
stl_gui_target_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:luxury_clothes } } add = variable_map(stl_eff_export|flag:luxury_clothes) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:luxury_clothes } } subtract = variable_map(stl_eff_import|flag:luxury_clothes) } }
stl_gui_target_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:luxury_furniture } } add = variable_map(stl_eff_export|flag:luxury_furniture) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:luxury_furniture } } subtract = variable_map(stl_eff_import|flag:luxury_furniture) } }
stl_gui_target_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:automobiles } } add = variable_map(stl_eff_export|flag:automobiles) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:automobiles } } subtract = variable_map(stl_eff_import|flag:automobiles) } }
stl_gui_target_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:telephones } } add = variable_map(stl_eff_export|flag:telephones) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:telephones } } subtract = variable_map(stl_eff_import|flag:telephones) } }
stl_gui_target_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:radios } } add = variable_map(stl_eff_export|flag:radios) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:radios } } subtract = variable_map(stl_eff_import|flag:radios) } }
stl_gui_target_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_eff_export key = flag:fine_art } } add = variable_map(stl_eff_export|flag:fine_art) } if = { limit = { is_key_in_variable_map = { name = stl_eff_import key = flag:fine_art } } subtract = variable_map(stl_eff_import|flag:fine_art) } }

# === access (whichever side is active; at most one is) ===
stl_gui_access_grain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:grain } } add = variable_map(stl_access_export|flag:grain) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:grain } } add = variable_map(stl_access_import|flag:grain) } }
stl_gui_access_fish = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fish } } add = variable_map(stl_access_export|flag:fish) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fish } } add = variable_map(stl_access_import|flag:fish) } }
stl_gui_access_fabric = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fabric } } add = variable_map(stl_access_export|flag:fabric) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fabric } } add = variable_map(stl_access_import|flag:fabric) } }
stl_gui_access_wood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:wood } } add = variable_map(stl_access_export|flag:wood) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:wood } } add = variable_map(stl_access_import|flag:wood) } }
stl_gui_access_groceries = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:groceries } } add = variable_map(stl_access_export|flag:groceries) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:groceries } } add = variable_map(stl_access_import|flag:groceries) } }
stl_gui_access_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:clothes } } add = variable_map(stl_access_export|flag:clothes) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:clothes } } add = variable_map(stl_access_import|flag:clothes) } }
stl_gui_access_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:furniture } } add = variable_map(stl_access_export|flag:furniture) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:furniture } } add = variable_map(stl_access_import|flag:furniture) } }
stl_gui_access_paper = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:paper } } add = variable_map(stl_access_export|flag:paper) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:paper } } add = variable_map(stl_access_import|flag:paper) } }
stl_gui_access_merchant_marine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:merchant_marine } } add = variable_map(stl_access_export|flag:merchant_marine) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:merchant_marine } } add = variable_map(stl_access_import|flag:merchant_marine) } }
stl_gui_access_coal = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:coal } } add = variable_map(stl_access_export|flag:coal) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:coal } } add = variable_map(stl_access_import|flag:coal) } }
stl_gui_access_iron = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:iron } } add = variable_map(stl_access_export|flag:iron) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:iron } } add = variable_map(stl_access_import|flag:iron) } }
stl_gui_access_lead = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:lead } } add = variable_map(stl_access_export|flag:lead) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:lead } } add = variable_map(stl_access_import|flag:lead) } }
stl_gui_access_sulfur = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:sulfur } } add = variable_map(stl_access_export|flag:sulfur) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:sulfur } } add = variable_map(stl_access_import|flag:sulfur) } }
stl_gui_access_hardwood = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:hardwood } } add = variable_map(stl_access_export|flag:hardwood) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:hardwood } } add = variable_map(stl_access_import|flag:hardwood) } }
stl_gui_access_rubber = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:rubber } } add = variable_map(stl_access_export|flag:rubber) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:rubber } } add = variable_map(stl_access_import|flag:rubber) } }
stl_gui_access_oil = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:oil } } add = variable_map(stl_access_export|flag:oil) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:oil } } add = variable_map(stl_access_import|flag:oil) } }
stl_gui_access_silk = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:silk } } add = variable_map(stl_access_export|flag:silk) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:silk } } add = variable_map(stl_access_import|flag:silk) } }
stl_gui_access_dye = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:dye } } add = variable_map(stl_access_export|flag:dye) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:dye } } add = variable_map(stl_access_import|flag:dye) } }
stl_gui_access_clippers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:clippers } } add = variable_map(stl_access_export|flag:clippers) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:clippers } } add = variable_map(stl_access_import|flag:clippers) } }
stl_gui_access_steamers = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:steamers } } add = variable_map(stl_access_export|flag:steamers) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:steamers } } add = variable_map(stl_access_import|flag:steamers) } }
stl_gui_access_glass = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:glass } } add = variable_map(stl_access_export|flag:glass) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:glass } } add = variable_map(stl_access_import|flag:glass) } }
stl_gui_access_fertilizer = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fertilizer } } add = variable_map(stl_access_export|flag:fertilizer) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fertilizer } } add = variable_map(stl_access_import|flag:fertilizer) } }
stl_gui_access_tools = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tools } } add = variable_map(stl_access_export|flag:tools) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tools } } add = variable_map(stl_access_import|flag:tools) } }
stl_gui_access_steel = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:steel } } add = variable_map(stl_access_export|flag:steel) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:steel } } add = variable_map(stl_access_import|flag:steel) } }
stl_gui_access_engines = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:engines } } add = variable_map(stl_access_export|flag:engines) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:engines } } add = variable_map(stl_access_import|flag:engines) } }
stl_gui_access_explosives = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:explosives } } add = variable_map(stl_access_export|flag:explosives) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:explosives } } add = variable_map(stl_access_import|flag:explosives) } }
stl_gui_access_ammunition = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:ammunition } } add = variable_map(stl_access_export|flag:ammunition) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:ammunition } } add = variable_map(stl_access_import|flag:ammunition) } }
stl_gui_access_small_arms = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:small_arms } } add = variable_map(stl_access_export|flag:small_arms) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:small_arms } } add = variable_map(stl_access_import|flag:small_arms) } }
stl_gui_access_artillery = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:artillery } } add = variable_map(stl_access_export|flag:artillery) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:artillery } } add = variable_map(stl_access_import|flag:artillery) } }
stl_gui_access_tanks = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tanks } } add = variable_map(stl_access_export|flag:tanks) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tanks } } add = variable_map(stl_access_import|flag:tanks) } }
stl_gui_access_aeroplanes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:aeroplanes } } add = variable_map(stl_access_export|flag:aeroplanes) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:aeroplanes } } add = variable_map(stl_access_import|flag:aeroplanes) } }
stl_gui_access_meat = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:meat } } add = variable_map(stl_access_export|flag:meat) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:meat } } add = variable_map(stl_access_import|flag:meat) } }
stl_gui_access_fruit = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fruit } } add = variable_map(stl_access_export|flag:fruit) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fruit } } add = variable_map(stl_access_import|flag:fruit) } }
stl_gui_access_sugar = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:sugar } } add = variable_map(stl_access_export|flag:sugar) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:sugar } } add = variable_map(stl_access_import|flag:sugar) } }
stl_gui_access_tobacco = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tobacco } } add = variable_map(stl_access_export|flag:tobacco) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tobacco } } add = variable_map(stl_access_import|flag:tobacco) } }
stl_gui_access_liquor = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:liquor } } add = variable_map(stl_access_export|flag:liquor) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:liquor } } add = variable_map(stl_access_import|flag:liquor) } }
stl_gui_access_wine = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:wine } } add = variable_map(stl_access_export|flag:wine) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:wine } } add = variable_map(stl_access_import|flag:wine) } }
stl_gui_access_tea = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:tea } } add = variable_map(stl_access_export|flag:tea) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:tea } } add = variable_map(stl_access_import|flag:tea) } }
stl_gui_access_coffee = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:coffee } } add = variable_map(stl_access_export|flag:coffee) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:coffee } } add = variable_map(stl_access_import|flag:coffee) } }
stl_gui_access_opium = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:opium } } add = variable_map(stl_access_export|flag:opium) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:opium } } add = variable_map(stl_access_import|flag:opium) } }
stl_gui_access_porcelain = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:porcelain } } add = variable_map(stl_access_export|flag:porcelain) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:porcelain } } add = variable_map(stl_access_import|flag:porcelain) } }
stl_gui_access_luxury_clothes = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:luxury_clothes } } add = variable_map(stl_access_export|flag:luxury_clothes) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:luxury_clothes } } add = variable_map(stl_access_import|flag:luxury_clothes) } }
stl_gui_access_luxury_furniture = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:luxury_furniture } } add = variable_map(stl_access_export|flag:luxury_furniture) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:luxury_furniture } } add = variable_map(stl_access_import|flag:luxury_furniture) } }
stl_gui_access_automobiles = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:automobiles } } add = variable_map(stl_access_export|flag:automobiles) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:automobiles } } add = variable_map(stl_access_import|flag:automobiles) } }
stl_gui_access_telephones = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:telephones } } add = variable_map(stl_access_export|flag:telephones) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:telephones } } add = variable_map(stl_access_import|flag:telephones) } }
stl_gui_access_radios = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:radios } } add = variable_map(stl_access_export|flag:radios) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:radios } } add = variable_map(stl_access_import|flag:radios) } }
stl_gui_access_fine_art = { value = 0 if = { limit = { is_key_in_variable_map = { name = stl_access_export key = flag:fine_art } } add = variable_map(stl_access_export|flag:fine_art) } if = { limit = { is_key_in_variable_map = { name = stl_access_import key = flag:fine_art } } add = variable_map(stl_access_import|flag:fine_art) } }
