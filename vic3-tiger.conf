# vic3-tiger configuration for SaltyTransport.
# Reference: https://github.com/amtep/tiger/blob/main/filter.md

languages = {
	check = "english"
}

filter = {
	trigger = {
		# Variable maps (game 1.13.7+) are not yet known to tiger v1.19.0;
		# every report mentioning them is a false positive until tiger
		# gains support. Remove this once tiger understands maps.
		NOT = { text = "variable_map" }
	}
}
